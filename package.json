{
  "name": "go-mini-app",
  "private": true,
  "version": "1.0.0",
  "description": "Static Go mini app with canonical tsumego data exports",
  "scripts": {
    "build:tsumego": "node scripts/build-tsumego-exports.js",
    "validate:tsumego": "node scripts/validate-tsumego.js",
    "refresh:tsumego": "npm run build:tsumego && npm run validate:tsumego",
    "check:script": "node --check script.js",
    "test:upload": "node --test scripts/test-tsumego-upload.js cloudflare/worker.test.mjs",
    "test:verifier": "python3 -m unittest test_go_tsumego_verifier.py",
    "cloudflare:dev": "npx wrangler dev --config cloudflare/wrangler.jsonc",
    "cloudflare:deploy": "npx wrangler deploy --config cloudflare/wrangler.jsonc",
    "check": "npm run validate:tsumego && npm run check:script && npm run test:upload && npm run test:verifier"
  }
}
